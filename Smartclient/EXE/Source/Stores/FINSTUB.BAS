'Finstub.bas
'07Nov01 JN Added FinStub.bas as a temporary measure to get over compilation problem with Beta Test Build. dblRound function should be moved to CoreLib.bas after this.
'01Jun02 All/CKJ option explicit, defint a-z and saved as text instead of binary

Option Explicit
DefInt A-Z

Function DblRound (TargetNum As Variant, DecPlaces%, AlwaysDown%) As Double

End Function

